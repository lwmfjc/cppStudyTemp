#include "cat.h"

Cat::Cat(const std::string& fur_style, const std::string& description)
    : Feline(fur_style, description)
{
    std::cout << "Cat::Cat() invoked!" << std::endl;
}

Cat::~Cat()
{
    std::cout << "Cat::~Cat() invoked!" << std::endl;
}

