#include <iostream>
// #include "person.h"
// #include "player.h"
// #include "nurse.h"
#include "engineer.h"
#include "civilengineer.h"

int main()
{
  // Person p1("myname", 12, "Snow addr");
  // std::cout << "player:" << p1 << std::endl;

  // std::cout << "---" << std::endl;
  // Player p;
  // p.m_full_name = "skdf Joack";
  // // p.m_age=3;
  // // p.m_address="ss";
  // Nurse nurse;
  // nurse.m_full_name = "skdf Joack";//protected
  // Engineer eng1;
  CivilEngineer ce1;
  ce1.build_road();
  // std::cout << ce1.get_address() << std::endl;
  std::cout << "done!" << std::endl;

  return 0;
}