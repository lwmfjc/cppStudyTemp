#include <iostream>
//其实这里并不需要常量，只不过为了演示
#include "constants.h"
#include "constants.h"
#include "cylinder.h" 
#include "cylinder.h" 

int main()
{
  Cylinder cylinder1(10, 10);
  std::cout << cylinder1.volume() << std::endl;
  return 0;
}