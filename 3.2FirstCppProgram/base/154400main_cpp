#include <iostream>
#include "compare.h"

int main()
{
  int x{5};
  int y{12};
  int result = max(x, y);
  std::cout << "max: " << result << std::endl;
  std::cout << "min: " << min(x, y) << std::endl;
  return 0;
}
